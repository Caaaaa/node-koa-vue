const config = {
    port: 3000,
    database: {
        DATABASE: 'node_koa',
        USERNAME: 'root',
        PASSWORD: 'nodekoa',
        PORT: '3306',
        HOST: '127.0.0.1'
    }
}
module.exports = config
